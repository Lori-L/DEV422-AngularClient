export class subclassObject {
    subclassIndex: string = '';

    subclassChoices: boolean = false;
    subclassSelections?: string[][][]; //used to store subclass-specific user choices, when they are present
}